# ❓ Часто задаваемые вопросы (FAQ)

## 🚀 Установка и запуск

### Q: Как узнать версию Python?
```bash
python --version
# или
python3 --version
```
Требуется Python 3.8 или выше.

### Q: Что делать если команда `python` не найдена?
**Windows:** Переустановите Python с галочкой "Add Python to PATH"
**Linux/Mac:** Используйте `python3` вместо `python`

### Q: Ошибка "pip не найден"
```bash
# Windows
python -m pip install -r requirements.txt

# Linux/Mac
python3 -m pip install -r requirements.txt
```

### Q: Как запустить бота в фоне (Linux)?
```bash
nohup python3 main.py &
```
Или используйте `screen`:
```bash
screen -S shopbot
python3 main.py
# Нажмите Ctrl+A, затем D для отсоединения
```

---

## ⚙️ Настройка

### Q: Где взять BOT_TOKEN?
1. Найдите [@BotFather](https://t.me/BotFather)
2. Отправьте `/newbot`
3. Следуйте инструкциям
4. Скопируйте токен вида: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`

### Q: Как узнать свой Telegram ID?
1. Найдите [@userinfobot](https://t.me/userinfobot)
2. Отправьте `/start`
3. Скопируйте ваш ID (только цифры)

### Q: Где получить данные ЮKassa?
1. Зарегистрируйтесь на [yookassa.ru](https://yookassa.ru/)
2. Создайте магазин (для теста - тестовый)
3. В личном кабинете:
   - **ShopID** - в настройках магазина
   - **Secret Key** - в разделе "Интеграция"

### Q: Файл .env не создаётся
`.env` - это обычный текстовый файл без расширения.
**Windows:** Создайте `.env.txt` и переименуйте в `.env`
**Linux/Mac:** `touch .env`

### Q: Бот не читает .env файл
Убедитесь:
- Файл называется именно `.env` (не `.env.txt`)
- Файл находится в той же папке, что и `main.py`
- Нет пробелов вокруг знака `=` в .env

---

## 💳 Платежи

### Q: Как протестировать оплату?
1. Используйте **тестовый магазин** ЮKassa
2. Используйте тестовые карты:
   - **Успешная оплата:** `5555 5555 5555 4444`
   - **Отклонённая:** `5555 5555 5555 5599`
   - CVV: любой (например `123`)
   - Срок: любая будущая дата

### Q: Оплата не проходит
Проверьте:
1. ✅ Правильность `YUKASSA_TOKEN` и `YUKASSA_SHOP_ID`
2. ✅ Магазин активен в ЮKassa
3. ✅ Настроены методы оплаты в ЮKassa
4. ✅ Используете правильный тип магазина (тестовый/боевой)

### Q: Можно ли использовать другую платёжную систему?
Да! Измените файл `services/payment.py`:
- Для Robokassa
- Для СберПэй
- Для других систем с API

### Q: Как изменить валюту?
В `services/payment.py` найдите:
```python
"currency": "RUB"
```
Замените на: `"USD"`, `"EUR"` и т.д.

---

## 🛒 Товары и каталог

### Q: Как добавить товар?
1. `/admin` → "➕ Добавить товар"
2. Введите название
3. Введите описание
4. Введите цену
5. **Выберите тип:**
   - 📝 Текст/Ключ - для ключей, паролей
   - 📎 Файл - для файлов (ZIP, EXE, PDF и др.)
6. Загрузите товар

### Q: Какие типы товаров поддерживаются?
**Текст/Ключ:**
- Ключи активации
- Логины и пароли
- Коды доступа
- Ссылки

**Файлы (до 50 МБ):**
- Архивы: ZIP, RAR, 7Z
- Программы: EXE, APK, MSI
- Документы: PDF, DOC, XLS
- Медиа: MP3, MP4, JPG
- Любые другие форматы

📖 Подробнее: `FILES_GUIDE.md`

### Q: Формат загрузки товаров
**Для текста** - каждый товар с новой строки:
```
KEY-1111-2222-3333
KEY-4444-5555-6666
ACCOUNT:login:password
```

**Для файлов** - отправьте файл как документ

### Q: Как дозагрузить товары?
`/admin` → "📦 Управление товарами" → выберите товар → "📦 Загрузить товар"

**Для текста:** новые ключи добавятся к существующим
**Для файла:** старый файл заменится на новый

### Q: Товар не выдаётся
Проверьте:
1. В стоке есть товары (не закончились)
2. Оплата прошла успешно
3. Пользователь нажал "Проверить оплату"
4. **Для файлов:** размер не превышает 50 МБ

### Q: Как изменить порядок товаров?
Товары сортируются по ID (время добавления). Для изменения порядка нужно пересоздать товары.

### Q: Можно ли продавать один файл много раз?
Да! Файл хранится на серверах Telegram и может быть продан неограниченному количеству покупателей.

### Q: Что делать с файлами больше 50 МБ?
Варианты:
- Сжать в архив с максимальным сжатием
- Разделить на несколько частей
- Загрузить на облако (Google Drive, Dropbox) и продавать ссылку как текстовый товар

---

## 👑 Админ-панель

### Q: Не могу открыть /admin
Убедитесь:
- В `.env` указан правильный `ADMIN_ID`
- Это **ваш** Telegram ID (не ID бота!)
- Нет пробелов в .env: `ADMIN_ID=123456789`

### Q: Как добавить несколько админов?
Измените `config.py`:
```python
ADMIN_IDS = [123456789, 987654321]
```
И в `handlers/admin.py`:
```python
def is_admin(user_id: int) -> bool:
    return user_id in ADMIN_IDS
```

### Q: Можно ли дать админу ограниченные права?
Да, создайте дополнительные роли:
```python
SUPER_ADMIN = 123456789
MANAGERS = [111111111, 222222222]
```

---

## 📊 Статистика и заказы

### Q: Где посмотреть статистику?
`/admin` → "📊 Статистика"

Показывает:
- Общую выручку
- Количество заказов
- Топ-5 товаров

### Q: Как экспортировать заказы?
В текущей версии - вручную из базы данных:
```bash
sqlite3 shop.db "SELECT * FROM orders" > orders.csv
```

### Q: Как посмотреть все заказы?
`/admin` → "📋 Все заказы"

Показывает последние 10 заказов.

---

## 🔧 Технические вопросы

### Q: База данных не создаётся
Проверьте права на запись в папке с ботом:
```bash
# Linux/Mac
chmod 755 .
```

### Q: Ошибка импорта модулей
Убедитесь:
1. Во всех папках есть `__init__.py`
2. Запускаете из корневой папки проекта
3. Установлены все зависимости

### Q: Бот падает при запуске
Проверьте логи в консоли:
```python
# В main.py установите уровень DEBUG
logging.basicConfig(level=logging.DEBUG)
```

### Q: Как обновить бота?
1. Остановите бота (Ctrl+C)
2. Замените файлы кода на новые
3. **НЕ ТРОГАЙТЕ** `.env` и `shop.db`
4. Запустите снова

### Q: Потерял доступ к базе данных
Создайте резервную копию:
```bash
cp shop.db shop_backup.db
```
Восстановление:
```bash
cp shop_backup.db shop.db
```

---

## 📝 Кастомизация

### Q: Как изменить текст приветствия?
Откройте `handlers/user.py`, найдите:
```python
START_TEXT = """
👋 <b>Добро пожаловать!</b>
...
"""
```

### Q: Как добавить новые кнопки?
В `keyboards/user_kb.py` измените функцию `main_menu_kb()`:
```python
keyboard = [
    [InlineKeyboardButton(text="🎁 Акции", callback_data="promo")],
    ...
]
```

### Q: Можно ли добавить фото товара?
Да! В `handlers/user.py` добавьте:
```python
await callback.message.answer_photo(
    photo="URL_или_file_id",
    caption=text,
    reply_markup=product_kb(product_id)
)
```

### Q: Как изменить дизайн кнопок?
Измените эмодзи в файлах `keyboards/`:
```python
text="🛒 Каталог"  # Было
text="📦 Магазин"  # Стало
```

---

## 🔐 Безопасность

### Q: Безопасно ли хранить токены в .env?
Да, если:
- `.env` не загружен в Git/GitHub
- Файл недоступен извне на сервере
- Используете `.gitignore`

### Q: Кто-то может украсть данные?
Нет, если:
- Не публикуете `.env` файл
- Не даёте доступ к серверу посторонним
- Регулярно обновляете токены

### Q: Как защитить базу данных?
```bash
# Linux/Mac
chmod 600 shop.db
```
Только владелец файла сможет читать/писать.

---

## 🚨 Решение проблем

### Бот не отвечает
1. Проверьте, запущен ли бот (консоль должна быть активна)
2. Проверьте интернет-соединение
3. Убедитесь, что токен правильный

### Бот зависает
1. Перезапустите бота (Ctrl+C, затем снова запустите)
2. Проверьте логи на ошибки
3. Убедитесь, что хватает RAM

### Оплата создаётся, но товар не выдаётся
1. Проверьте сток (есть ли товары)
2. Проверьте статус заказа в базе
3. Проверьте логи бота

### ModuleNotFoundError: No module named 'aiogram'
```bash
pip install -r requirements.txt --force-reinstall
```

### sqlite3.OperationalError: database is locked
Закройте все программы, использующие базу данных.

---

## 💡 Полезные советы

### Автоматический перезапуск (Linux)
Создайте systemd service:
```bash
sudo nano /etc/systemd/system/shopbot.service
```
Содержимое:
```ini
[Unit]
Description=Telegram Shop Bot
After=network.target

[Service]
Type=simple
User=your_user
WorkingDirectory=/path/to/bot
ExecStart=/usr/bin/python3 main.py
Restart=always

[Install]
WantedBy=multi-user.target
```
Запуск:
```bash
sudo systemctl enable shopbot
sudo systemctl start shopbot
```

### Мониторинг логов
```bash
tail -f logs.txt
```

### Резервное копирование
Создайте скрипт backup.sh:
```bash
#!/bin/bash
DATE=$(date +%Y%m%d_%H%M%S)
cp shop.db backups/shop_$DATE.db
echo "Backup created: shop_$DATE.db"
```

---

## 📞 Куда обратиться?

Если не нашли ответ:
1. Проверьте `README.md` - полная документация
2. Проверьте `QUICKSTART.md` - быстрая установка
3. Проверьте логи бота в консоли
4. Проверьте issues на GitHub (если проект там)

---

**Удачи в использовании бота! 🎉**