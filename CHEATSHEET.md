# 📋 Шпаргалка команд

## ⚡ Быстрый старт

```bash
# 1. Установка зависимостей
pip install -r requirements.txt

# 2. Создание .env из примера
cp .env.example .env
# Отредактируйте .env своими данными

# 3. Запуск
python main.py
```

## 📁 Поддерживаемые типы товаров

### 📝 Текст/Ключ
```
KEY-1111-2222-3333
KEY-4444-5555-6666
login:password
https://example.com/file
```

### 📎 Файл (до 50 МБ)
```
Архивы:    .zip .rar .7z
Программы: .exe .apk .msi
Документы: .pdf .doc .txt
Медиа:     .mp3 .mp4 .jpg
Другие:    любые форматы
```

📖 Подробнее: `FILES_GUIDE.md`

## 🤖 Команды бота

### Для пользователей
| Команда | Описание |
|---------|----------|
| `/start` | Запуск бота, главное меню |

### Для администратора
| Команда | Описание |
|---------|----------|
| `/admin` | Открыть админ-панель |

---

## 👑 Админ-панель (кнопки)

```
/admin
├── ➕ Добавить товар
│   ├── Название
│   ├── Описание
│   ├── Цена
│   ├── Тип товара
│   │   ├── 📝 Текст/Ключ
│   │   └── 📎 Файл
│   └── Загрузка товара
│       ├── Текст: ключи построчно
│       └── Файл: отправить документ
│
├── 📦 Управление товарами
│   └── [Выбор товара]
│       ├── ✏️ Изменить цену
│       ├── 📝 Изменить описание
│       ├── 📦 Загрузить товар
│       │   ├── Текст: добавить ключи
│       │   └── Файл: заменить файл
│       └── 🗑 Удалить товар
│
├── 📊 Статистика
│   ├── Общая выручка
│   ├── Количество заказов
│   └── Топ-5 товаров
│
└── 📋 Все заказы
    └── Последние 10 заказов
```

---

## 🔧 Системные команды

### Установка

```bash
# Windows
pip install -r requirements.txt

# Linux/Mac
pip3 install -r requirements.txt

# Обновление зависимостей
pip install -r requirements.txt --upgrade
```

### Запуск

```bash
# Windows
python main.py

# Linux/Mac
python3 main.py

# Фоновый запуск (Linux)
nohup python3 main.py &

# В screen (Linux)
screen -S shopbot
python3 main.py
# Ctrl+A, затем D для отсоединения
```

### Остановка

```bash
# В консоли
Ctrl + C

# Фоновый процесс (Linux)
pkill -f main.py

# Screen
screen -r shopbot
Ctrl + C
```

---

## 📁 Структура файлов

```
telegram_shop_bot/
├── main.py              # Запуск
├── config.py            # Конфиг
├── .env                 # Настройки (создать!)
├── requirements.txt     # Зависимости
│
├── database/
│   └── models.py        # База данных
│
├── handlers/
│   ├── user.py          # Пользователи
│   └── admin.py         # Админ
│
├── keyboards/
│   ├── user_kb.py       # Кнопки пользователей
│   └── admin_kb.py      # Кнопки админа
│
└── services/
    └── payment.py       # ЮKassa
```

---

## 🔑 Переменные .env

```env
BOT_TOKEN=123:ABC...          # @BotFather
ADMIN_ID=123456789            # @userinfobot
YUKASSA_TOKEN=live_ABC...     # yookassa.ru
YUKASSA_SHOP_ID=123456        # yookassa.ru
DATABASE_PATH=shop.db         # Путь к БД
```

---

## 💳 Тестовые карты ЮKassa

| Результат | Номер карты |
|-----------|-------------|
| ✅ Успех | `5555 5555 5555 4444` |
| ❌ Отклонено | `5555 5555 5555 5599` |

- **CVV:** любой (123)
- **Срок:** любая будущая дата
- **3D-Secure:** 12345

---

## 📊 База данных

### Просмотр данных

```bash
# Открыть базу
sqlite3 shop.db

# Просмотр товаров
SELECT * FROM products;

# Просмотр заказов
SELECT * FROM orders;

# Выход
.quit
```

### Резервное копирование

```bash
# Создать бэкап
cp shop.db shop_backup_$(date +%Y%m%d).db

# Восстановить
cp shop_backup_20260120.db shop.db
```

### Очистка данных

```bash
sqlite3 shop.db "DELETE FROM orders;"
sqlite3 shop.db "DELETE FROM products;"
```

---

## 📝 Быстрое редактирование

### Изменить приветствие
Файл: `handlers/user.py`
```python
START_TEXT = """Ваш текст"""
```

### Изменить поддержку
Файл: `handlers/user.py`
```python
SUPPORT_TEXT = """Ваши контакты"""
```

### Добавить кнопку в меню
Файл: `keyboards/user_kb.py`
```python
def main_menu_kb():
    keyboard = [
        [InlineKeyboardButton(text="🎁 Новая кнопка", 
                            callback_data="new_button")],
        # ... остальные кнопки
    ]
```

---

## 🔍 Отладка

### Включить подробные логи
Файл: `main.py`
```python
logging.basicConfig(level=logging.DEBUG)
```

### Проверить импорты
```bash
python -c "from handlers.user import router; print('OK')"
```

### Тест платежей
Файл: `services/payment.py`
```python
# Добавьте print для отладки
print(f"Payment created: {payment_data}")
```

---

## 🚨 Частые ошибки

| Ошибка | Решение |
|--------|---------|
| `BOT_TOKEN не установлен` | Проверьте .env файл |
| `ModuleNotFoundError` | `pip install -r requirements.txt` |
| `sqlite3.OperationalError` | Закройте программы, использующие БД |
| Админка не работает | Проверьте ADMIN_ID в .env |
| Оплата не создаётся | Проверьте YUKASSA данные |

---

## 🔄 Обновление

```bash
# 1. Остановить бота
Ctrl + C

# 2. Бэкап
cp shop.db shop_backup.db
cp .env .env.backup

# 3. Заменить файлы кода
# (оставьте .env и shop.db)

# 4. Обновить зависимости
pip install -r requirements.txt --upgrade

# 5. Запустить
python main.py
```

---

## 📞 Полезные ссылки

| Ресурс | Ссылка |
|--------|--------|
| Создать бота | [@BotFather](https://t.me/BotFather) |
| Узнать ID | [@userinfobot](https://t.me/userinfobot) |
| ЮKassa | [yookassa.ru](https://yookassa.ru) |
| Aiogram Docs | [docs.aiogram.dev](https://docs.aiogram.dev) |

---

## 💡 Полезные трюки

### Автозапуск при перезагрузке (Linux)

```bash
# Добавить в crontab
crontab -e

# Добавить строку
@reboot cd /path/to/bot && python3 main.py
```

### Проверка статуса бота

```bash
ps aux | grep main.py
```

### Просмотр логов в реальном времени

```bash
python main.py 2>&1 | tee bot.log
```

---

## 🎯 Чеклист перед продакшеном

- [ ] Изменены все тексты приветствия
- [ ] Указаны правильные контакты поддержки
- [ ] Настроен боевой (не тестовый) ЮKassa
- [ ] Добавлены товары в каталог
- [ ] Протестирована покупка
- [ ] Настроен автозапуск
- [ ] Настроены бэкапы
- [ ] Удалена отладочная информация

---

**Сохраните эту шпаргалку для быстрого доступа! 📌**