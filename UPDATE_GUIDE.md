# 🔄 Руководство по обновлению до версии 1.1.0

## 📋 Что нового в версии 1.1.0

### Главное обновление
✨ **Поддержка файловых товаров!**

Теперь вы можете продавать:
- 📎 Файлы (ZIP, EXE, PDF, APK и др.) до 50 МБ
- 📝 Текст/Ключи (как раньше)

---

## ⚡ Быстрое обновление (5 минут)

### Шаг 1: Создайте резервную копию (1 мин)

```bash
# Остановите бота
Ctrl + C

# Создайте бэкап
cp shop.db shop_backup_$(date +%Y%m%d).db
cp .env .env.backup
```

### Шаг 2: Обновите файлы (2 мин)

Замените эти файлы на новые версии:
- ✅ `database/models.py`
- ✅ `handlers/user.py`
- ✅ `handlers/admin.py`
- ✅ `keyboards/admin_kb.py`

**НЕ ТРОГАЙТЕ:**
- ❌ `.env` - оставьте как есть
- ❌ `shop.db` - база данных

### Шаг 3: Запустите бота (1 мин)

```bash
python main.py
```

База данных обновится автоматически!

### Шаг 4: Проверьте работу (1 мин)

1. Отправьте `/admin`
2. Попробуйте добавить товар
3. Вы должны увидеть выбор типа: 📝 Текст или 📎 Файл

✅ Готово! Обновление завершено!

---

## 📝 Детальная инструкция

### Изменения в базе данных

Автоматически добавится новое поле:
```sql
ALTER TABLE products ADD COLUMN product_type TEXT DEFAULT 'text'
```

**Важно:** Все существующие товары получат тип `'text'` и будут работать как раньше!

### Изменённые файлы

#### 1. `database/models.py`
**Что изменилось:**
- Добавлено поле `product_type` в таблицу products
- Функция `add_product()` принимает параметр `product_type`
- Функция `update_product()` может обновлять `product_type`

**Обратная совместимость:** ✅ Да
- Старые товары автоматически получают тип 'text'
- Все старые функции работают как раньше

#### 2. `handlers/user.py`
**Что изменилось:**
- Функция `show_product()` отображает тип товара
- Функция `buy_product()` проверяет тип при подсчёте стока
- Функция `check_payment_status()` выдаёт файл или текст в зависимости от типа

**Обратная совместимость:** ✅ Да
- Если `product_type` не указан, используется 'text'

#### 3. `handlers/admin.py`
**Что изменилось:**
- Добавлено состояние `waiting_product_type`
- Добавлены обработчики выбора типа товара
- Функция добавления стока различает текст и файлы
- Детали товара показывают тип

**Обратная совместимость:** ✅ Да

#### 4. `keyboards/admin_kb.py`
**Что изменилось:**
- Список товаров показывает эмодзи типа (📝/📎)
- Подсчёт стока учитывает тип товара

**Обратная совместимость:** ✅ Да

---

## 🔍 Проверка после обновления

### Тест 1: Существующие товары
```
1. Откройте каталог
2. Проверьте, что старые товары видны
3. Попробуйте купить старый товар
4. Убедитесь, что товар выдаётся как раньше
```
✅ Должно работать без изменений

### Тест 2: Новый текстовый товар
```
1. /admin → Добавить товар
2. Заполните данные
3. Выберите: 📝 Текст/Ключ
4. Добавьте ключи
5. Проверьте покупку
```
✅ Должно работать как в v1.0.0

### Тест 3: Новый файловый товар
```
1. /admin → Добавить товар
2. Заполните данные
3. Выберите: 📎 Файл
4. Отправьте файл
5. Проверьте покупку
6. Убедитесь, что файл отправляется
```
✅ Новый функционал!

---

## ⚠️ Возможные проблемы

### Проблема: База данных не обновляется

**Симптомы:**
- Ошибка при добавлении товара
- Нет выбора типа товара

**Решение:**
```bash
# Остановите бота
Ctrl + C

# Удалите старую базу (после бэкапа!)
rm shop.db

# Запустите бота - создастся новая база
python main.py
```

### Проблема: Старые товары не работают

**Решение:**
```bash
# Откройте базу данных
sqlite3 shop.db

# Обновите тип для всех товаров
UPDATE products SET product_type = 'text' WHERE product_type IS NULL;

# Выйдите
.quit
```

### Проблема: Файлы не отправляются

**Возможные причины:**
1. Файл больше 50 МБ
2. Неправильный file_id
3. Файл был удалён из Telegram

**Решение:**
- Загрузите файл заново через "Загрузить товар"

---

## 🔙 Откат к версии 1.0.0

Если что-то пошло не так:

```bash
# Остановите бота
Ctrl + C

# Восстановите бэкап
cp shop_backup_20260120.db shop.db
cp .env.backup .env

# Верните старые файлы кода
# (должны быть сохранены отдельно)

# Запустите
python main.py
```

---

## 📊 Миграция данных

### Хотите преобразовать текстовые товары в файловые?

#### Вариант 1: Создать новый товар
Просто создайте новый товар типа "файл" и удалите старый.

#### Вариант 2: Обновить в базе данных
```bash
sqlite3 shop.db

# Посмотрите товары
SELECT id, name, product_type FROM products;

# Измените тип для конкретного товара
UPDATE products SET product_type = 'file' WHERE id = 1;

# Обновите сток (file_id файла)
UPDATE products SET stock = 'BQACAgIAAxkBAAI...' WHERE id = 1;

.quit
```

⚠️ **Осторожно:** Убедитесь, что в `stock` указан правильный file_id!

---

## 📚 Дополнительная документация

После обновления ознакомьтесь с:
- 📎 `FILES_GUIDE.md` - Полное руководство по работе с файлами
- 📝 `CHANGELOG.md` - Детальный список изменений
- ❓ `FAQ.md` - Обновлённые вопросы и ответы

---

## ✅ Чеклист обновления

- [ ] Создан бэкап базы данных
- [ ] Создан бэкап .env файла
- [ ] Бот остановлен
- [ ] Обновлены файлы кода
- [ ] Бот запущен
- [ ] База данных обновилась автоматически
- [ ] Старые товары работают
- [ ] Можно создать текстовый товар
- [ ] Можно создать файловый товар
- [ ] Тестовая покупка файла успешна
- [ ] Прочитан FILES_GUIDE.md

---

## 🎉 Поздравляем!

Вы успешно обновились до версии 1.1.0!

Теперь вы можете продавать:
- 📝 Текстовые товары (как раньше)
- 📎 Файловые товары (новое!)

Используйте `FILES_GUIDE.md` для освоения новых возможностей.

**Удачных продаж! 🚀**